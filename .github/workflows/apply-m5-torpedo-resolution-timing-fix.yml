name: Apply M5 torpedo resolution timing fix

on:
  push:
    branches:
      - fix/m5-weapon-ux-soft-envelopes
    paths:
      - Tools/apply_m5_torpedo_resolution_timing_fix.py
      - .github/workflows/apply-m5-torpedo-resolution-timing-fix.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/m5-weapon-ux-soft-envelopes
          fetch-depth: 0
      - name: Apply production fix
        run: python Tools/apply_m5_torpedo_resolution_timing_fix.py
      - name: Validate patch
        run: git diff --check && git diff --stat
      - name: Commit fix and remove temporary scaffolding
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm Tools/apply_m5_torpedo_resolution_timing_fix.py .github/workflows/apply-m5-torpedo-resolution-timing-fix.yml
          git add -A
          git commit -m "fix: preserve torpedo impact state through its event frame"
          git push origin HEAD:fix/m5-weapon-ux-soft-envelopes
